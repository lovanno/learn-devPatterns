//Exercise 1 - Must be point-free (Uses CartItems Array)
//EX Use: const price = getTotalPrice(cart); // '$44.20'
const getTotalPrice = () => {};


//Exercise 2 - Must be point-free (Uses CartTotal Array)
//EX Use: const cheapestItem = getCheapestItem(cart); // 'apple'
const getCheapestItem = () => {};


//Exercise 3 - Given a maximum price and menu, return the 3 top-rated meals for that price or less 
// Must be point-free (Uses menuItems Array)
//EX Use: 
/*const firstPerfectMeal = pipe(
  getTop3MealsFor(20),
  filter(
    both(isVegetarian, isLactoseFree)
  ),
  head
);*/
const getTop3MealsFor = () => {};


//Exercise 4 - Given a list of employee salaries, find the median monthly paycheck above $100,000
//Must be point-free (uses employeesList)
//EX: Useconst medianPaycheck = getMedianPaycheck(employees) // $141,000
const getMedianPaycheck = () => {};



//Exercise 5 - Given a list of credit scores, return a list of reviews. Must be Point Free
/*      If the score’s at or above 800, return “{score} is excellent!”
        If the score’s at or above 700, return “{score} is good”
        If the score’s at or above 650, return “{score} is fair”
        If the score’s at or below 649, return “{score} is poor”*/

// Ex: Use const ratings = [740, 550, 681, 805]; ['740 is good', '550 is poor', ' 681 is fair', '805 is excellent!']
//Use ratings from data.js
const reviewCreditScores = () => {};
